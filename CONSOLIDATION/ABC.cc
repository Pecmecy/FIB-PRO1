#include <iostream>
using namespace std;

int main() {
    int a, b, c, aux;
    cin >> x >> y >> z;
    string orden;
    cin >> orden
    if ((a > b) and (a > c)) {
        if (c > b) {
            aux = c;
            c = a;
            a = b;
            b = aux;
        }
        else {
            aux = c;
            c = a;
            b = a;
            a = aux;
        }
    }
    
}
