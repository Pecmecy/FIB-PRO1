#include <iostream>
using namespace std;

//Pre: carácter (letra, dígito o marca de puntiación) y un string.
//Post: cout en que se da "true" si el carácter es el string dado o "false" si no lo es.

void print_line(char c, string s, bool b) {
    cout << s << "('" << c << "') = ";
    if (b) cout << "true" << endl;
    else cout << "false" << endl;
}

int main() {
    char c;
    cin >> c;
    string s;
    bool is_string = false;
    while (cin >> s) {
        if (string == letter) {
            i
        }
    }
}